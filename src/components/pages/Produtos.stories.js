import React from "react";
import Produtos from "./Produtos";
import styled from "styled-components";

export default {
  title: "Components/Pages/Produtos",
  component: Produtos,
};

export const usage = () => <Produtos />;
